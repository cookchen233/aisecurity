// Code generated by ent, DO NOT EDIT.

package ent

import (
	"aisecurity/ent/deptadmin"
	"aisecurity/ent/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// DeptAdminQuery is the builder for querying DeptAdmin entities.
type DeptAdminQuery struct {
	config
	ctx        *QueryContext
	order      []deptadmin.OrderOption
	inters     []Interceptor
	predicates []predicate.DeptAdmin
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DeptAdminQuery builder.
func (daq *DeptAdminQuery) Where(ps ...predicate.DeptAdmin) *DeptAdminQuery {
	daq.predicates = append(daq.predicates, ps...)
	return daq
}

// Limit the number of records to be returned by this query.
func (daq *DeptAdminQuery) Limit(limit int) *DeptAdminQuery {
	daq.ctx.Limit = &limit
	return daq
}

// Offset to start from.
func (daq *DeptAdminQuery) Offset(offset int) *DeptAdminQuery {
	daq.ctx.Offset = &offset
	return daq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (daq *DeptAdminQuery) Unique(unique bool) *DeptAdminQuery {
	daq.ctx.Unique = &unique
	return daq
}

// Order specifies how the records should be ordered.
func (daq *DeptAdminQuery) Order(o ...deptadmin.OrderOption) *DeptAdminQuery {
	daq.order = append(daq.order, o...)
	return daq
}

// First returns the first DeptAdmin entity from the query.
// Returns a *NotFoundError when no DeptAdmin was found.
func (daq *DeptAdminQuery) First(ctx context.Context) (*DeptAdmin, error) {
	nodes, err := daq.Limit(1).All(setContextOp(ctx, daq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{deptadmin.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (daq *DeptAdminQuery) FirstX(ctx context.Context) *DeptAdmin {
	node, err := daq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DeptAdmin ID from the query.
// Returns a *NotFoundError when no DeptAdmin ID was found.
func (daq *DeptAdminQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = daq.Limit(1).IDs(setContextOp(ctx, daq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{deptadmin.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (daq *DeptAdminQuery) FirstIDX(ctx context.Context) int {
	id, err := daq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DeptAdmin entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DeptAdmin entity is found.
// Returns a *NotFoundError when no DeptAdmin entities are found.
func (daq *DeptAdminQuery) Only(ctx context.Context) (*DeptAdmin, error) {
	nodes, err := daq.Limit(2).All(setContextOp(ctx, daq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{deptadmin.Label}
	default:
		return nil, &NotSingularError{deptadmin.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (daq *DeptAdminQuery) OnlyX(ctx context.Context) *DeptAdmin {
	node, err := daq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DeptAdmin ID in the query.
// Returns a *NotSingularError when more than one DeptAdmin ID is found.
// Returns a *NotFoundError when no entities are found.
func (daq *DeptAdminQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = daq.Limit(2).IDs(setContextOp(ctx, daq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{deptadmin.Label}
	default:
		err = &NotSingularError{deptadmin.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (daq *DeptAdminQuery) OnlyIDX(ctx context.Context) int {
	id, err := daq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DeptAdmins.
func (daq *DeptAdminQuery) All(ctx context.Context) ([]*DeptAdmin, error) {
	ctx = setContextOp(ctx, daq.ctx, "All")
	if err := daq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DeptAdmin, *DeptAdminQuery]()
	return withInterceptors[[]*DeptAdmin](ctx, daq, qr, daq.inters)
}

// AllX is like All, but panics if an error occurs.
func (daq *DeptAdminQuery) AllX(ctx context.Context) []*DeptAdmin {
	nodes, err := daq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DeptAdmin IDs.
func (daq *DeptAdminQuery) IDs(ctx context.Context) (ids []int, err error) {
	if daq.ctx.Unique == nil && daq.path != nil {
		daq.Unique(true)
	}
	ctx = setContextOp(ctx, daq.ctx, "IDs")
	if err = daq.Select(deptadmin.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (daq *DeptAdminQuery) IDsX(ctx context.Context) []int {
	ids, err := daq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (daq *DeptAdminQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, daq.ctx, "Count")
	if err := daq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, daq, querierCount[*DeptAdminQuery](), daq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (daq *DeptAdminQuery) CountX(ctx context.Context) int {
	count, err := daq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (daq *DeptAdminQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, daq.ctx, "Exist")
	switch _, err := daq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (daq *DeptAdminQuery) ExistX(ctx context.Context) bool {
	exist, err := daq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DeptAdminQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (daq *DeptAdminQuery) Clone() *DeptAdminQuery {
	if daq == nil {
		return nil
	}
	return &DeptAdminQuery{
		config:     daq.config,
		ctx:        daq.ctx.Clone(),
		order:      append([]deptadmin.OrderOption{}, daq.order...),
		inters:     append([]Interceptor{}, daq.inters...),
		predicates: append([]predicate.DeptAdmin{}, daq.predicates...),
		// clone intermediate query.
		sql:  daq.sql.Clone(),
		path: daq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (daq *DeptAdminQuery) GroupBy(field string, fields ...string) *DeptAdminGroupBy {
	daq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DeptAdminGroupBy{build: daq}
	grbuild.flds = &daq.ctx.Fields
	grbuild.label = deptadmin.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (daq *DeptAdminQuery) Select(fields ...string) *DeptAdminSelect {
	daq.ctx.Fields = append(daq.ctx.Fields, fields...)
	sbuild := &DeptAdminSelect{DeptAdminQuery: daq}
	sbuild.label = deptadmin.Label
	sbuild.flds, sbuild.scan = &daq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DeptAdminSelect configured with the given aggregations.
func (daq *DeptAdminQuery) Aggregate(fns ...AggregateFunc) *DeptAdminSelect {
	return daq.Select().Aggregate(fns...)
}

func (daq *DeptAdminQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range daq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, daq); err != nil {
				return err
			}
		}
	}
	for _, f := range daq.ctx.Fields {
		if !deptadmin.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if daq.path != nil {
		prev, err := daq.path(ctx)
		if err != nil {
			return err
		}
		daq.sql = prev
	}
	return nil
}

func (daq *DeptAdminQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DeptAdmin, error) {
	var (
		nodes = []*DeptAdmin{}
		_spec = daq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DeptAdmin).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DeptAdmin{config: daq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, daq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (daq *DeptAdminQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := daq.querySpec()
	_spec.Node.Columns = daq.ctx.Fields
	if len(daq.ctx.Fields) > 0 {
		_spec.Unique = daq.ctx.Unique != nil && *daq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, daq.driver, _spec)
}

func (daq *DeptAdminQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(deptadmin.Table, deptadmin.Columns, sqlgraph.NewFieldSpec(deptadmin.FieldID, field.TypeInt))
	_spec.From = daq.sql
	if unique := daq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if daq.path != nil {
		_spec.Unique = true
	}
	if fields := daq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, deptadmin.FieldID)
		for i := range fields {
			if fields[i] != deptadmin.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := daq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := daq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := daq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := daq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (daq *DeptAdminQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(daq.driver.Dialect())
	t1 := builder.Table(deptadmin.Table)
	columns := daq.ctx.Fields
	if len(columns) == 0 {
		columns = deptadmin.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if daq.sql != nil {
		selector = daq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if daq.ctx.Unique != nil && *daq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range daq.predicates {
		p(selector)
	}
	for _, p := range daq.order {
		p(selector)
	}
	if offset := daq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := daq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// DeptAdminGroupBy is the group-by builder for DeptAdmin entities.
type DeptAdminGroupBy struct {
	selector
	build *DeptAdminQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (dagb *DeptAdminGroupBy) Aggregate(fns ...AggregateFunc) *DeptAdminGroupBy {
	dagb.fns = append(dagb.fns, fns...)
	return dagb
}

// Scan applies the selector query and scans the result into the given value.
func (dagb *DeptAdminGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dagb.build.ctx, "GroupBy")
	if err := dagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DeptAdminQuery, *DeptAdminGroupBy](ctx, dagb.build, dagb, dagb.build.inters, v)
}

func (dagb *DeptAdminGroupBy) sqlScan(ctx context.Context, root *DeptAdminQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(dagb.fns))
	for _, fn := range dagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*dagb.flds)+len(dagb.fns))
		for _, f := range *dagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*dagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DeptAdminSelect is the builder for selecting fields of DeptAdmin entities.
type DeptAdminSelect struct {
	*DeptAdminQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (das *DeptAdminSelect) Aggregate(fns ...AggregateFunc) *DeptAdminSelect {
	das.fns = append(das.fns, fns...)
	return das
}

// Scan applies the selector query and scans the result into the given value.
func (das *DeptAdminSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, das.ctx, "Select")
	if err := das.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DeptAdminQuery, *DeptAdminSelect](ctx, das.DeptAdminQuery, das, das.inters, v)
}

func (das *DeptAdminSelect) sqlScan(ctx context.Context, root *DeptAdminQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(das.fns))
	for _, fn := range das.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*das.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := das.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
