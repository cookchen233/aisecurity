// Code generated by ent, DO NOT EDIT.

package dao

import (
	"aisecurity/ent/dao/admin"
	"aisecurity/ent/dao/predicate"
	"aisecurity/ent/dao/risk"
	"aisecurity/ent/dao/riskcategory"
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// RiskCategoryQuery is the builder for querying RiskCategory entities.
type RiskCategoryQuery struct {
	config
	ctx                  *QueryContext
	order                []riskcategory.OrderOption
	inters               []Interceptor
	predicates           []predicate.RiskCategory
	withCreator          *AdminQuery
	withUpdator          *AdminQuery
	withRiskRiskCategory *RiskQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RiskCategoryQuery builder.
func (rcq *RiskCategoryQuery) Where(ps ...predicate.RiskCategory) *RiskCategoryQuery {
	rcq.predicates = append(rcq.predicates, ps...)
	return rcq
}

// Limit the number of records to be returned by this query.
func (rcq *RiskCategoryQuery) Limit(limit int) *RiskCategoryQuery {
	rcq.ctx.Limit = &limit
	return rcq
}

// Offset to start from.
func (rcq *RiskCategoryQuery) Offset(offset int) *RiskCategoryQuery {
	rcq.ctx.Offset = &offset
	return rcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rcq *RiskCategoryQuery) Unique(unique bool) *RiskCategoryQuery {
	rcq.ctx.Unique = &unique
	return rcq
}

// Order specifies how the records should be ordered.
func (rcq *RiskCategoryQuery) Order(o ...riskcategory.OrderOption) *RiskCategoryQuery {
	rcq.order = append(rcq.order, o...)
	return rcq
}

// QueryCreator chains the current query on the "creator" edge.
func (rcq *RiskCategoryQuery) QueryCreator() *AdminQuery {
	query := (&AdminClient{config: rcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(riskcategory.Table, riskcategory.FieldID, selector),
			sqlgraph.To(admin.Table, admin.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, riskcategory.CreatorTable, riskcategory.CreatorColumn),
		)
		fromU = sqlgraph.SetNeighbors(rcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUpdator chains the current query on the "updator" edge.
func (rcq *RiskCategoryQuery) QueryUpdator() *AdminQuery {
	query := (&AdminClient{config: rcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(riskcategory.Table, riskcategory.FieldID, selector),
			sqlgraph.To(admin.Table, admin.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, riskcategory.UpdatorTable, riskcategory.UpdatorColumn),
		)
		fromU = sqlgraph.SetNeighbors(rcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRiskRiskCategory chains the current query on the "risk_risk_category" edge.
func (rcq *RiskCategoryQuery) QueryRiskRiskCategory() *RiskQuery {
	query := (&RiskClient{config: rcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(riskcategory.Table, riskcategory.FieldID, selector),
			sqlgraph.To(risk.Table, risk.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, riskcategory.RiskRiskCategoryTable, riskcategory.RiskRiskCategoryColumn),
		)
		fromU = sqlgraph.SetNeighbors(rcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first RiskCategory entity from the query.
// Returns a *NotFoundError when no RiskCategory was found.
func (rcq *RiskCategoryQuery) First(ctx context.Context) (*RiskCategory, error) {
	nodes, err := rcq.Limit(1).All(setContextOp(ctx, rcq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{riskcategory.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rcq *RiskCategoryQuery) FirstX(ctx context.Context) *RiskCategory {
	node, err := rcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RiskCategory ID from the query.
// Returns a *NotFoundError when no RiskCategory ID was found.
func (rcq *RiskCategoryQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = rcq.Limit(1).IDs(setContextOp(ctx, rcq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{riskcategory.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rcq *RiskCategoryQuery) FirstIDX(ctx context.Context) int {
	id, err := rcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RiskCategory entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RiskCategory entity is found.
// Returns a *NotFoundError when no RiskCategory entities are found.
func (rcq *RiskCategoryQuery) Only(ctx context.Context) (*RiskCategory, error) {
	nodes, err := rcq.Limit(2).All(setContextOp(ctx, rcq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{riskcategory.Label}
	default:
		return nil, &NotSingularError{riskcategory.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rcq *RiskCategoryQuery) OnlyX(ctx context.Context) *RiskCategory {
	node, err := rcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RiskCategory ID in the query.
// Returns a *NotSingularError when more than one RiskCategory ID is found.
// Returns a *NotFoundError when no entities are found.
func (rcq *RiskCategoryQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = rcq.Limit(2).IDs(setContextOp(ctx, rcq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{riskcategory.Label}
	default:
		err = &NotSingularError{riskcategory.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rcq *RiskCategoryQuery) OnlyIDX(ctx context.Context) int {
	id, err := rcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RiskCategories.
func (rcq *RiskCategoryQuery) All(ctx context.Context) ([]*RiskCategory, error) {
	ctx = setContextOp(ctx, rcq.ctx, "All")
	if err := rcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RiskCategory, *RiskCategoryQuery]()
	return withInterceptors[[]*RiskCategory](ctx, rcq, qr, rcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rcq *RiskCategoryQuery) AllX(ctx context.Context) []*RiskCategory {
	nodes, err := rcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RiskCategory IDs.
func (rcq *RiskCategoryQuery) IDs(ctx context.Context) (ids []int, err error) {
	if rcq.ctx.Unique == nil && rcq.path != nil {
		rcq.Unique(true)
	}
	ctx = setContextOp(ctx, rcq.ctx, "IDs")
	if err = rcq.Select(riskcategory.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rcq *RiskCategoryQuery) IDsX(ctx context.Context) []int {
	ids, err := rcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rcq *RiskCategoryQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rcq.ctx, "Count")
	if err := rcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rcq, querierCount[*RiskCategoryQuery](), rcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rcq *RiskCategoryQuery) CountX(ctx context.Context) int {
	count, err := rcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rcq *RiskCategoryQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rcq.ctx, "Exist")
	switch _, err := rcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("dao: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rcq *RiskCategoryQuery) ExistX(ctx context.Context) bool {
	exist, err := rcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RiskCategoryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rcq *RiskCategoryQuery) Clone() *RiskCategoryQuery {
	if rcq == nil {
		return nil
	}
	return &RiskCategoryQuery{
		config:               rcq.config,
		ctx:                  rcq.ctx.Clone(),
		order:                append([]riskcategory.OrderOption{}, rcq.order...),
		inters:               append([]Interceptor{}, rcq.inters...),
		predicates:           append([]predicate.RiskCategory{}, rcq.predicates...),
		withCreator:          rcq.withCreator.Clone(),
		withUpdator:          rcq.withUpdator.Clone(),
		withRiskRiskCategory: rcq.withRiskRiskCategory.Clone(),
		// clone intermediate query.
		sql:  rcq.sql.Clone(),
		path: rcq.path,
	}
}

// WithCreator tells the query-builder to eager-load the nodes that are connected to
// the "creator" edge. The optional arguments are used to configure the query builder of the edge.
func (rcq *RiskCategoryQuery) WithCreator(opts ...func(*AdminQuery)) *RiskCategoryQuery {
	query := (&AdminClient{config: rcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rcq.withCreator = query
	return rcq
}

// WithUpdator tells the query-builder to eager-load the nodes that are connected to
// the "updator" edge. The optional arguments are used to configure the query builder of the edge.
func (rcq *RiskCategoryQuery) WithUpdator(opts ...func(*AdminQuery)) *RiskCategoryQuery {
	query := (&AdminClient{config: rcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rcq.withUpdator = query
	return rcq
}

// WithRiskRiskCategory tells the query-builder to eager-load the nodes that are connected to
// the "risk_risk_category" edge. The optional arguments are used to configure the query builder of the edge.
func (rcq *RiskCategoryQuery) WithRiskRiskCategory(opts ...func(*RiskQuery)) *RiskCategoryQuery {
	query := (&RiskClient{config: rcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rcq.withRiskRiskCategory = query
	return rcq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RiskCategory.Query().
//		GroupBy(riskcategory.FieldCreatedAt).
//		Aggregate(dao.Count()).
//		Scan(ctx, &v)
func (rcq *RiskCategoryQuery) GroupBy(field string, fields ...string) *RiskCategoryGroupBy {
	rcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RiskCategoryGroupBy{build: rcq}
	grbuild.flds = &rcq.ctx.Fields
	grbuild.label = riskcategory.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at"`
//	}
//
//	client.RiskCategory.Query().
//		Select(riskcategory.FieldCreatedAt).
//		Scan(ctx, &v)
func (rcq *RiskCategoryQuery) Select(fields ...string) *RiskCategorySelect {
	rcq.ctx.Fields = append(rcq.ctx.Fields, fields...)
	sbuild := &RiskCategorySelect{RiskCategoryQuery: rcq}
	sbuild.label = riskcategory.Label
	sbuild.flds, sbuild.scan = &rcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RiskCategorySelect configured with the given aggregations.
func (rcq *RiskCategoryQuery) Aggregate(fns ...AggregateFunc) *RiskCategorySelect {
	return rcq.Select().Aggregate(fns...)
}

func (rcq *RiskCategoryQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rcq.inters {
		if inter == nil {
			return fmt.Errorf("dao: uninitialized interceptor (forgotten import dao/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rcq); err != nil {
				return err
			}
		}
	}
	for _, f := range rcq.ctx.Fields {
		if !riskcategory.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("dao: invalid field %q for query", f)}
		}
	}
	if rcq.path != nil {
		prev, err := rcq.path(ctx)
		if err != nil {
			return err
		}
		rcq.sql = prev
	}
	return nil
}

func (rcq *RiskCategoryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RiskCategory, error) {
	var (
		nodes       = []*RiskCategory{}
		_spec       = rcq.querySpec()
		loadedTypes = [3]bool{
			rcq.withCreator != nil,
			rcq.withUpdator != nil,
			rcq.withRiskRiskCategory != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RiskCategory).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RiskCategory{config: rcq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := rcq.withCreator; query != nil {
		if err := rcq.loadCreator(ctx, query, nodes, nil,
			func(n *RiskCategory, e *Admin) { n.Edges.Creator = e }); err != nil {
			return nil, err
		}
	}
	if query := rcq.withUpdator; query != nil {
		if err := rcq.loadUpdator(ctx, query, nodes, nil,
			func(n *RiskCategory, e *Admin) { n.Edges.Updator = e }); err != nil {
			return nil, err
		}
	}
	if query := rcq.withRiskRiskCategory; query != nil {
		if err := rcq.loadRiskRiskCategory(ctx, query, nodes,
			func(n *RiskCategory) { n.Edges.RiskRiskCategory = []*Risk{} },
			func(n *RiskCategory, e *Risk) { n.Edges.RiskRiskCategory = append(n.Edges.RiskRiskCategory, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (rcq *RiskCategoryQuery) loadCreator(ctx context.Context, query *AdminQuery, nodes []*RiskCategory, init func(*RiskCategory), assign func(*RiskCategory, *Admin)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*RiskCategory)
	for i := range nodes {
		fk := nodes[i].CreatedBy
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(admin.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "created_by" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (rcq *RiskCategoryQuery) loadUpdator(ctx context.Context, query *AdminQuery, nodes []*RiskCategory, init func(*RiskCategory), assign func(*RiskCategory, *Admin)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*RiskCategory)
	for i := range nodes {
		fk := nodes[i].UpdatedBy
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(admin.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "updated_by" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (rcq *RiskCategoryQuery) loadRiskRiskCategory(ctx context.Context, query *RiskQuery, nodes []*RiskCategory, init func(*RiskCategory), assign func(*RiskCategory, *Risk)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*RiskCategory)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(risk.FieldRiskCategoryID)
	}
	query.Where(predicate.Risk(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(riskcategory.RiskRiskCategoryColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.RiskCategoryID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "risk_category_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (rcq *RiskCategoryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rcq.querySpec()
	_spec.Node.Columns = rcq.ctx.Fields
	if len(rcq.ctx.Fields) > 0 {
		_spec.Unique = rcq.ctx.Unique != nil && *rcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rcq.driver, _spec)
}

func (rcq *RiskCategoryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(riskcategory.Table, riskcategory.Columns, sqlgraph.NewFieldSpec(riskcategory.FieldID, field.TypeInt))
	_spec.From = rcq.sql
	if unique := rcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rcq.path != nil {
		_spec.Unique = true
	}
	if fields := rcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, riskcategory.FieldID)
		for i := range fields {
			if fields[i] != riskcategory.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if rcq.withCreator != nil {
			_spec.Node.AddColumnOnce(riskcategory.FieldCreatedBy)
		}
		if rcq.withUpdator != nil {
			_spec.Node.AddColumnOnce(riskcategory.FieldUpdatedBy)
		}
	}
	if ps := rcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rcq *RiskCategoryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rcq.driver.Dialect())
	t1 := builder.Table(riskcategory.Table)
	columns := rcq.ctx.Fields
	if len(columns) == 0 {
		columns = riskcategory.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rcq.sql != nil {
		selector = rcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rcq.ctx.Unique != nil && *rcq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range rcq.predicates {
		p(selector)
	}
	for _, p := range rcq.order {
		p(selector)
	}
	if offset := rcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RiskCategoryGroupBy is the group-by builder for RiskCategory entities.
type RiskCategoryGroupBy struct {
	selector
	build *RiskCategoryQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rcgb *RiskCategoryGroupBy) Aggregate(fns ...AggregateFunc) *RiskCategoryGroupBy {
	rcgb.fns = append(rcgb.fns, fns...)
	return rcgb
}

// Scan applies the selector query and scans the result into the given value.
func (rcgb *RiskCategoryGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rcgb.build.ctx, "GroupBy")
	if err := rcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RiskCategoryQuery, *RiskCategoryGroupBy](ctx, rcgb.build, rcgb, rcgb.build.inters, v)
}

func (rcgb *RiskCategoryGroupBy) sqlScan(ctx context.Context, root *RiskCategoryQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rcgb.fns))
	for _, fn := range rcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rcgb.flds)+len(rcgb.fns))
		for _, f := range *rcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RiskCategorySelect is the builder for selecting fields of RiskCategory entities.
type RiskCategorySelect struct {
	*RiskCategoryQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rcs *RiskCategorySelect) Aggregate(fns ...AggregateFunc) *RiskCategorySelect {
	rcs.fns = append(rcs.fns, fns...)
	return rcs
}

// Scan applies the selector query and scans the result into the given value.
func (rcs *RiskCategorySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rcs.ctx, "Select")
	if err := rcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RiskCategoryQuery, *RiskCategorySelect](ctx, rcs.RiskCategoryQuery, rcs, rcs.inters, v)
}

func (rcs *RiskCategorySelect) sqlScan(ctx context.Context, root *RiskCategoryQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rcs.fns))
	for _, fn := range rcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
