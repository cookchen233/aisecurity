// Code generated by ent, DO NOT EDIT.

package dao

import (
	"aisecurity/ent/dao/admin"
	"aisecurity/ent/dao/riskcategory"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// RiskCategory is the model entity for the RiskCategory schema.
type RiskCategory struct {
	config `json:"-" validate:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 创建时间
	CreateTime time.Time `json:"create_time"`
	// 创建者
	CreatorID int `json:"creator_id"`
	// 删除时间
	DeleteTime *time.Time `json:"delete_time"`
	// 最后更新者
	UpdaterID int `json:"updater_id"`
	// 最后更新时间
	UpdateTime time.Time `json:"update_time"`
	// 名称
	Name string `json:"name" validate:"required"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the RiskCategoryQuery when eager-loading is set.
	Edges        RiskCategoryEdges `json:"edges"`
	selectValues sql.SelectValues
}

// RiskCategoryEdges holds the relations/edges for other nodes in the graph.
type RiskCategoryEdges struct {
	// Creator holds the value of the creator edge.
	Creator *Admin `json:"creator,omitempty"`
	// Updater holds the value of the updater edge.
	Updater *Admin `json:"updater,omitempty"`
	// Risk holds the value of the risk edge.
	Risk []*Risk `json:"risk,omitempty"`
	// Sweep holds the value of the sweep edge.
	Sweep []*Sweep `json:"sweep,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [4]bool
}

// CreatorOrErr returns the Creator value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e RiskCategoryEdges) CreatorOrErr() (*Admin, error) {
	if e.loadedTypes[0] {
		if e.Creator == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: admin.Label}
		}
		return e.Creator, nil
	}
	return nil, &NotLoadedError{edge: "creator"}
}

// UpdaterOrErr returns the Updater value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e RiskCategoryEdges) UpdaterOrErr() (*Admin, error) {
	if e.loadedTypes[1] {
		if e.Updater == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: admin.Label}
		}
		return e.Updater, nil
	}
	return nil, &NotLoadedError{edge: "updater"}
}

// RiskOrErr returns the Risk value or an error if the edge
// was not loaded in eager-loading.
func (e RiskCategoryEdges) RiskOrErr() ([]*Risk, error) {
	if e.loadedTypes[2] {
		return e.Risk, nil
	}
	return nil, &NotLoadedError{edge: "risk"}
}

// SweepOrErr returns the Sweep value or an error if the edge
// was not loaded in eager-loading.
func (e RiskCategoryEdges) SweepOrErr() ([]*Sweep, error) {
	if e.loadedTypes[3] {
		return e.Sweep, nil
	}
	return nil, &NotLoadedError{edge: "sweep"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RiskCategory) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case riskcategory.FieldID, riskcategory.FieldCreatorID, riskcategory.FieldUpdaterID:
			values[i] = new(sql.NullInt64)
		case riskcategory.FieldName:
			values[i] = new(sql.NullString)
		case riskcategory.FieldCreateTime, riskcategory.FieldDeleteTime, riskcategory.FieldUpdateTime:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RiskCategory fields.
func (rc *RiskCategory) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case riskcategory.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			rc.ID = int(value.Int64)
		case riskcategory.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				rc.CreateTime = value.Time
			}
		case riskcategory.FieldCreatorID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field creator_id", values[i])
			} else if value.Valid {
				rc.CreatorID = int(value.Int64)
			}
		case riskcategory.FieldDeleteTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field delete_time", values[i])
			} else if value.Valid {
				rc.DeleteTime = new(time.Time)
				*rc.DeleteTime = value.Time
			}
		case riskcategory.FieldUpdaterID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field updater_id", values[i])
			} else if value.Valid {
				rc.UpdaterID = int(value.Int64)
			}
		case riskcategory.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				rc.UpdateTime = value.Time
			}
		case riskcategory.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				rc.Name = value.String
			}
		default:
			rc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RiskCategory.
// This includes values selected through modifiers, order, etc.
func (rc *RiskCategory) Value(name string) (ent.Value, error) {
	return rc.selectValues.Get(name)
}

// QueryCreator queries the "creator" edge of the RiskCategory entity.
func (rc *RiskCategory) QueryCreator() *AdminQuery {
	return NewRiskCategoryClient(rc.config).QueryCreator(rc)
}

// QueryUpdater queries the "updater" edge of the RiskCategory entity.
func (rc *RiskCategory) QueryUpdater() *AdminQuery {
	return NewRiskCategoryClient(rc.config).QueryUpdater(rc)
}

// QueryRisk queries the "risk" edge of the RiskCategory entity.
func (rc *RiskCategory) QueryRisk() *RiskQuery {
	return NewRiskCategoryClient(rc.config).QueryRisk(rc)
}

// QuerySweep queries the "sweep" edge of the RiskCategory entity.
func (rc *RiskCategory) QuerySweep() *SweepQuery {
	return NewRiskCategoryClient(rc.config).QuerySweep(rc)
}

// Update returns a builder for updating this RiskCategory.
// Note that you need to call RiskCategory.Unwrap() before calling this method if this RiskCategory
// was returned from a transaction, and the transaction was committed or rolled back.
func (rc *RiskCategory) Update() *RiskCategoryUpdateOne {
	return NewRiskCategoryClient(rc.config).UpdateOne(rc)
}

// Unwrap unwraps the RiskCategory entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (rc *RiskCategory) Unwrap() *RiskCategory {
	_tx, ok := rc.config.driver.(*txDriver)
	if !ok {
		panic("dao: RiskCategory is not a transactional entity")
	}
	rc.config.driver = _tx.drv
	return rc
}

// String implements the fmt.Stringer.
func (rc *RiskCategory) String() string {
	var builder strings.Builder
	builder.WriteString("RiskCategory(")
	builder.WriteString(fmt.Sprintf("id=%v, ", rc.ID))
	builder.WriteString("create_time=")
	builder.WriteString(rc.CreateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("creator_id=")
	builder.WriteString(fmt.Sprintf("%v", rc.CreatorID))
	builder.WriteString(", ")
	if v := rc.DeleteTime; v != nil {
		builder.WriteString("delete_time=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("updater_id=")
	builder.WriteString(fmt.Sprintf("%v", rc.UpdaterID))
	builder.WriteString(", ")
	builder.WriteString("update_time=")
	builder.WriteString(rc.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(rc.Name)
	builder.WriteByte(')')
	return builder.String()
}

// RiskCategories is a parsable slice of RiskCategory.
type RiskCategories []*RiskCategory
