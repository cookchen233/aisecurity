// Code generated by ent, DO NOT EDIT.

package dao

import (
	"aisecurity/ent/dao/admin"
	"aisecurity/ent/dao/riskcategory"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// RiskCategory is the model entity for the RiskCategory schema.
type RiskCategory struct {
	config `json:"-" validate:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 创建时间
	CreatedAt time.Time `json:"created_at"`
	// 创建者
	CreatedBy int `json:"created_by"`
	// 删除时间
	DeletedAt *time.Time `json:"deleted_at"`
	// 最后更新者
	UpdatedBy int `json:"updated_by"`
	// 最后更新时间
	UpdatedAt time.Time `json:"updated_at"`
	// 标题
	Title string `json:"title" validate:"required"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the RiskCategoryQuery when eager-loading is set.
	Edges        RiskCategoryEdges `json:"edges"`
	selectValues sql.SelectValues
}

// RiskCategoryEdges holds the relations/edges for other nodes in the graph.
type RiskCategoryEdges struct {
	// Creator holds the value of the creator edge.
	Creator *Admin `json:"creator,omitempty"`
	// Updator holds the value of the updator edge.
	Updator *Admin `json:"updator,omitempty"`
	// RiskRiskCategory holds the value of the risk_risk_category edge.
	RiskRiskCategory []*Risk `json:"risk_risk_category,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// CreatorOrErr returns the Creator value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e RiskCategoryEdges) CreatorOrErr() (*Admin, error) {
	if e.loadedTypes[0] {
		if e.Creator == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: admin.Label}
		}
		return e.Creator, nil
	}
	return nil, &NotLoadedError{edge: "creator"}
}

// UpdatorOrErr returns the Updator value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e RiskCategoryEdges) UpdatorOrErr() (*Admin, error) {
	if e.loadedTypes[1] {
		if e.Updator == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: admin.Label}
		}
		return e.Updator, nil
	}
	return nil, &NotLoadedError{edge: "updator"}
}

// RiskRiskCategoryOrErr returns the RiskRiskCategory value or an error if the edge
// was not loaded in eager-loading.
func (e RiskCategoryEdges) RiskRiskCategoryOrErr() ([]*Risk, error) {
	if e.loadedTypes[2] {
		return e.RiskRiskCategory, nil
	}
	return nil, &NotLoadedError{edge: "risk_risk_category"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RiskCategory) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case riskcategory.FieldID, riskcategory.FieldCreatedBy, riskcategory.FieldUpdatedBy:
			values[i] = new(sql.NullInt64)
		case riskcategory.FieldTitle:
			values[i] = new(sql.NullString)
		case riskcategory.FieldCreatedAt, riskcategory.FieldDeletedAt, riskcategory.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RiskCategory fields.
func (rc *RiskCategory) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case riskcategory.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			rc.ID = int(value.Int64)
		case riskcategory.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				rc.CreatedAt = value.Time
			}
		case riskcategory.FieldCreatedBy:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field created_by", values[i])
			} else if value.Valid {
				rc.CreatedBy = int(value.Int64)
			}
		case riskcategory.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				rc.DeletedAt = new(time.Time)
				*rc.DeletedAt = value.Time
			}
		case riskcategory.FieldUpdatedBy:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field updated_by", values[i])
			} else if value.Valid {
				rc.UpdatedBy = int(value.Int64)
			}
		case riskcategory.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				rc.UpdatedAt = value.Time
			}
		case riskcategory.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				rc.Title = value.String
			}
		default:
			rc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RiskCategory.
// This includes values selected through modifiers, order, etc.
func (rc *RiskCategory) Value(name string) (ent.Value, error) {
	return rc.selectValues.Get(name)
}

// QueryCreator queries the "creator" edge of the RiskCategory entity.
func (rc *RiskCategory) QueryCreator() *AdminQuery {
	return NewRiskCategoryClient(rc.config).QueryCreator(rc)
}

// QueryUpdator queries the "updator" edge of the RiskCategory entity.
func (rc *RiskCategory) QueryUpdator() *AdminQuery {
	return NewRiskCategoryClient(rc.config).QueryUpdator(rc)
}

// QueryRiskRiskCategory queries the "risk_risk_category" edge of the RiskCategory entity.
func (rc *RiskCategory) QueryRiskRiskCategory() *RiskQuery {
	return NewRiskCategoryClient(rc.config).QueryRiskRiskCategory(rc)
}

// Update returns a builder for updating this RiskCategory.
// Note that you need to call RiskCategory.Unwrap() before calling this method if this RiskCategory
// was returned from a transaction, and the transaction was committed or rolled back.
func (rc *RiskCategory) Update() *RiskCategoryUpdateOne {
	return NewRiskCategoryClient(rc.config).UpdateOne(rc)
}

// Unwrap unwraps the RiskCategory entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (rc *RiskCategory) Unwrap() *RiskCategory {
	_tx, ok := rc.config.driver.(*txDriver)
	if !ok {
		panic("dao: RiskCategory is not a transactional entity")
	}
	rc.config.driver = _tx.drv
	return rc
}

// String implements the fmt.Stringer.
func (rc *RiskCategory) String() string {
	var builder strings.Builder
	builder.WriteString("RiskCategory(")
	builder.WriteString(fmt.Sprintf("id=%v, ", rc.ID))
	builder.WriteString("created_at=")
	builder.WriteString(rc.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_by=")
	builder.WriteString(fmt.Sprintf("%v", rc.CreatedBy))
	builder.WriteString(", ")
	if v := rc.DeletedAt; v != nil {
		builder.WriteString("deleted_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("updated_by=")
	builder.WriteString(fmt.Sprintf("%v", rc.UpdatedBy))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(rc.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("title=")
	builder.WriteString(rc.Title)
	builder.WriteByte(')')
	return builder.String()
}

// RiskCategories is a parsable slice of RiskCategory.
type RiskCategories []*RiskCategory
